asyncapi: '3.0.0'
info:
  title: Demo API
components:
  schemas:
    UserRef:
      $ref: '#/components/schemas/User'
    Example:
      enabled: true
      quotedEnabled: "true"
      count: 12
      quotedCount: "12"
      tags:
        - public
        - internal
